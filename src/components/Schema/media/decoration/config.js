import { ComType } from '@/config/data-source'

export const decorationConfig = {
  alias: '装饰器',
  icon: 'pushpin',
  type: ComType.com,
  attr: { w: 200, h: 200 },
  config: {
    img: 'decoration10',
  },
  apiData: {},
  events: {},
}
